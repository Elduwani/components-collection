import React from 'react';

const Default = () => {
    return (
        <div style={{
            color: `var(--green)`,
            margin: `30px 0`,
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center'
        }}>
            404 Page
        </div>
    );
}

export default Default;
